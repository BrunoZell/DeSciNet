# Print versions
docker --version
cs version
rustc --version
cargo --version
rustup --version
rustup override list
node --version
npm --version
yarn --version
jq --version
yq --version
ansible --version
git --version