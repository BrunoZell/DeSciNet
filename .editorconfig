# top-most EditorConfig file
root = true

# Unix-style newlines with a newline ending every file
[*]
end_of_line = lf
insert_final_newline = true

# Set encoding for specific file types without UTF-8 BOM
[*.{scala,sbt,json,yml,yaml,txt,sh,md, Dockerfile}]
charset = utf-8
