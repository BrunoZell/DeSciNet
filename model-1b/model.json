{
  "I": [
    ["H-longitude", "float"],
    ["H-latitude", "float"],
    ["epsilon", "float"],
    ["lambda", "float"]
  ],
  "J": [
    ["M-longitude", "float"],
    ["M-latitude", "float"]
  ],
  "f": [
    {
      "output": "H-longitude",
      "inputs": ["M-longitude", "epsilon", "lambda"],
      "expression": "latest(M-longitude, t) + epsilon * exp(-lambda * (t - latest(M-longitude, t).timestamp))"
    },
    {
      "output": "H-latitude",
      "inputs": ["M-latitude", "epsilon", "lambda"],
      "expression": "latest(M-latitude, t) + epsilon * exp(-lambda * (t - latest(M-latitude, t).timestamp))"
    },
    {
      "output": "epsilon",
      "inputs": [],
      "expression": "0.5"
    },
    {
      "output": "lambda",
      "inputs": [],
      "expression": "0.05"
    }
  ]
}